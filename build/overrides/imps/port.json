{
  "asset_dirs": [
    "personas",
    "references",
    "scripts",
    "templates"
  ],
  "asset_exclude": {
    "scripts/dispatch-banner.py": "Colour-codes the imp roster by Claude model name — `opus`/`sonnet`/`haiku` substring matches against a `model` field in the run state file. This build routes by tier (`cheap`/`standard`/`deep`) and its state file carries no `model` field, so the script would silently colour every imp the default green while re-introducing Claude model names as a runtime choice (contract item 23). Its only caller was the Workflow-driven Phase 3, which this directory's overrides replace with a foreground loop that has no roster to print.",
    "scripts/final-banner.py": "The end-of-run companion to dispatch-banner.py, excluded for the same reason: it colours by `opus`/`sonnet`/`haiku` and synthesizes a roster from a `model_counts` map that a tier-routed run never writes. Excluded with its caller, the Workflow `run_complete` checkpoint.",
    "scripts/imps-run.workflow.js": "The Claude Code Workflow runtime for the whole run — agent() dispatch, isolation: 'worktree', the Workflow resume model. Neither platform has a Workflow tool, so shipping it would present Claude mechanics as if they run there (contract item 16). The per-platform dispatch loop is prose, from this directory's overrides.",
    "scripts/run-ocr.sh": "The Claude-host read-only OCR review harness. It uses Claude Workflow wrapper mechanics and temporary configuration overrides, neither of which belongs in generated runtimes that already review inside their own runtime."
  },
  "asset_replacements": {
    "references/checklist-mode.md": [
      [
        "- Model = haiku for shell/grep checks; sonnet for items marked `[JUDGMENT — sonnet]`",
        "- Tier = `cheap` for shell/grep checks; `standard` for items marked `[JUDGMENT]`"
      ],
      [
        "For each task, spawn a `query` imp (haiku or sonnet, worktree-isolated=false) that:",
        "For each task, dispatch a `query` imp (`cheap` or `standard` tier, no worktree) that:"
      ],
      [
        "audits — dispatch them directly; no state file or Workflow script is involved unless",
        "audits — dispatch them directly; no state file or background run driver is involved unless"
      ],
      [
        "  write the state file and hand the run to the Workflow script (model-routed,",
        "  write the state file and hand the run to the dispatch loop (tier-routed,"
      ]
    ],
    "references/discussion-mode.md": [
      [
        "one obligation the Workflow script fulfills at finalize.*",
        "one obligation the run's finalize step fulfills.*"
      ],
      [
        "the source discussion whenever finalize is reached. **The Workflow script owns this** — it",
        "the source discussion whenever finalize is reached. **The finalize step owns this** — it"
      ]
    ],
    "references/persona-posting.md": [
      [
        "Phase 4 and the free-text run's Workflow script alike. Generalized on purpose: this file",
        "Phase 4 and the free-text run's own panel alike. Generalized on purpose: this file"
      ]
    ]
  },
  "manifest_overrides": {
    "description": "Swarm orchestration with serial tier-routed execution, deterministic gates, whole-repo audits, PR monitoring, and proof-led blast-radius analysis. (The Claude Code build drives the main run from a background Workflow script; here the skill drives it directly.)"
  },
  "source_hash_pins": {
    "scripts/imps-run.workflow.js": "e77347e2d5cff0a1001d58ca55f6dd898787756a8a2439d7133e61d6983941a0"
  }
}
