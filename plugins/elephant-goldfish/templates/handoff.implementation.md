# Implementation brief — {{TITLE}}

<!--
  Generated by /elephant-goldfish:thinking on {{GENERATED_AT}} for topic `{{TOPIC_SLUG}}`.
  This file IS the input to step 3. Hand it to /imps:imps, or paste it as the first message
  of a fresh session. Do not edit it by hand — edit discovery.md or spec.md and re-render.
-->

Verify `handoff.manifest.json` against this file and the discovery/spec inputs before implementation; use `scripts/render_handoff.py <topic-slug> --check` from the Elephant Goldfish installation. Re-render mismatches from the reviewed inputs.

## Run it

```bash
/imps:imps Read thinking/{{TOPIC_SLUG}}/handoff.md in full and implement what it describes. Treat the evaluation criteria as hard requirements and the auto-reject conditions as absolute. Do not re-propose any approach the problem definition rules out.
```

Or paste everything below as the first message of a fresh session.

---

You are implementing a change. Everything you need is in this document; there is no prior
conversation to draw on and no opportunity to ask follow-up questions. Preserve requirement IDs, constraints and rejected options. Implement and verify against the original acceptance contract; retain evidence on the tested revision. Unverified requirements remain incomplete. A fresh context is a design choice to evaluate, not a guarantee of independence.

Two sections follow. The first defines the problem, the current state of the code, and the
constraints. The second is the rubric the change will be reviewed against by someone who did
not write it.

Follow the plan as specified. Where it leaves something genuinely undetermined, state the
assumption you made and carry on — do not stop to ask.

---

## Problem definition

{{DISCOVERY}}

---

## Evaluation criteria

{{SPEC}}

---

## Instructions

1. Read both sections above in full before touching any file.
2. Implement the change. Meet every pass/fail criterion; treat the auto-reject conditions as
   absolute — one hit fails the whole change regardless of other merits.
3. Respect the stated scope. Files listed as out of scope stay untouched; work that arrived
   by association rather than need does not belong in this diff.
4. Do not re-propose any approach the problem definition rules out. The reasons are recorded
   there; they are not an invitation to relitigate.
5. Run the verification commands named in the evaluation criteria and report their real
   output — including failures. A green summary over a red run is the one unrecoverable
   failure mode here.
